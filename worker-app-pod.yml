apiVersion: v1
Kind: Pod
metadata:
  name: worker-app-pod
  labels:
    name: worker-app-pod
    app: demo-voitng-app
spec:
  containers:
  - name: worker-app
    image: dockersamples/examplevotingapp_worker
